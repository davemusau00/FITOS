# FITOS restore drill — 20 August 2026

Environment: isolated local Docker network, PostgreSQL 18 Alpine, `fitos-backup:restore-drill`.

Source preparation:

- migrations applied from an empty database;
- demo tenant seeded;
- PostgreSQL-backed Playwright pilot river passed;
- source contained lead/member, service/schedule, membership/credits, bookings, payment, and attendance truth.

Backup and restore evidence:

- `pg_dump` custom-format stream encrypted with a newly generated age recipient;
- SHA-256 sidecar validated before decrypting;
- encrypted backup copied byte-for-byte to a separate off-site-simulation Docker volume;
- guarded restore completed into a second clean PostgreSQL database;
- restore and table verification completed in under 15 seconds locally.

Verified restored row counts:

| Table | Rows |
| --- | ---: |
| `tenants` | 2 |
| `members` | 1 |
| `bookings` | 2 |
| `member_memberships` | 1 |
| `credit_ledger` | 4 |
| `payment_transactions` | 1 |
| `attendance_records` | 1 |

Result: passed.

This local drill proves the scripts, encryption, checksum, restore, and business-record verification path. Before pilot release, repeat it on staging with the off-site directory backed by infrastructure physically separate from the VPS; a second local Docker volume is not sufficient off-server durability evidence.
